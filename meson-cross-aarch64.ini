[constants]
arch = 'aarch64'

[binaries]
c = arch + '-linux-gnu-gcc'
cpp = arch + '-linux-gnu-g++'
ar = arch + '-linux-gnu-gcc-ar'
strip = arch + '-linux-gnu-strip'
pkg-config = arch + '-linux-gnu-pkg-config'

[host_machine]
system = 'linux'
cpu_family = 'aarch64'
cpu = 'aarch64'
endian = 'little'
